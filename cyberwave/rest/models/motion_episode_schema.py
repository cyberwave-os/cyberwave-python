# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MotionEpisodeSchema(BaseModel):
    """
    Canonical dense, time-indexed rollout of joint positions.  @sync cyberwave-frontend/lib/types.ts:MotionEpisode  ADR 0001 (\"Harmonized playground\") pins this shape as the single domain-level motion artefact flowing through every player, adapter, and dispatch path. LeRobot is *an encoding*, not our domain type — so nothing in this schema mentions it. The matching wire tag on model-run responses is ``output_format: \"motion_episode\"``; the legacy ``\"lerobot_episode\"`` tag is accepted on read for one release (see :class:`MLModelRunResultSchema`).  Invariants:   * ``len(observation_state) == len(action)`` when both arrays     are non-empty — one row per frame.   * ``observation_state[i]`` is length ``len(joint_names)`` for     every ``i`` (URDF-joint order, radians for revolute joints).   * ``ee_positions[i] = (x, y, z)`` in metres, in the asset's     URDF root frame. Optional; populated for EE-only replays     where ``observation_state`` is empty.   * ``fps`` > 0.
    """ # noqa: E501
    fps: Union[StrictFloat, StrictInt]
    joint_names: List[StrictStr]
    observation_state: List[List[Union[StrictFloat, StrictInt]]]
    action: Optional[List[List[Union[StrictFloat, StrictInt]]]] = None
    ee_positions: Optional[List[List[Union[StrictFloat, StrictInt]]]] = None
    instruction: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["fps", "joint_names", "observation_state", "action", "ee_positions", "instruction"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MotionEpisodeSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if action (nullable) is None
        # and model_fields_set contains the field
        if self.action is None and "action" in self.model_fields_set:
            _dict['action'] = None

        # set to None if ee_positions (nullable) is None
        # and model_fields_set contains the field
        if self.ee_positions is None and "ee_positions" in self.model_fields_set:
            _dict['ee_positions'] = None

        # set to None if instruction (nullable) is None
        # and model_fields_set contains the field
        if self.instruction is None and "instruction" in self.model_fields_set:
            _dict['instruction'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MotionEpisodeSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "fps": obj.get("fps"),
            "joint_names": obj.get("joint_names"),
            "observation_state": obj.get("observation_state"),
            "action": obj.get("action"),
            "ee_positions": obj.get("ee_positions"),
            "instruction": obj.get("instruction")
        })
        return _obj


