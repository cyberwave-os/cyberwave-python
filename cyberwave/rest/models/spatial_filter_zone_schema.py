# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class SpatialFilterZoneSchema(BaseModel):
    """
    A single armed ``spatial_filter`` zone scoped to a twin.  Used by the read-only polygon overlay rendered on top of the twin's WebRTC stream so operators can see which zones are armed without opening every workflow individually.  Coordinates mirror ``spatial_filter.parameters.polygon`` from ``cyberwave-backend/src/lib/workflow_utils.py``: normalized [0..1] ``[[x, y], ...]`` floats. The ``polygon`` echo here is the raw, un-validated parameters value — clients should treat it as advisory and skip rendering when fewer than 3 vertices are present (the cloud executor will refuse to act on those anyway).
    """ # noqa: E501
    workflow_uuid: StrictStr
    workflow_name: StrictStr
    workflow_slug: Optional[StrictStr]
    node_uuid: StrictStr
    node_name: StrictStr
    polygon: List[List[Union[StrictFloat, StrictInt]]]
    point_mode: StrictStr
    classes: List[StrictStr]
    sensor_id: Optional[StrictStr]
    __properties: ClassVar[List[str]] = ["workflow_uuid", "workflow_name", "workflow_slug", "node_uuid", "node_name", "polygon", "point_mode", "classes", "sensor_id"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SpatialFilterZoneSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if workflow_slug (nullable) is None
        # and model_fields_set contains the field
        if self.workflow_slug is None and "workflow_slug" in self.model_fields_set:
            _dict['workflow_slug'] = None

        # set to None if sensor_id (nullable) is None
        # and model_fields_set contains the field
        if self.sensor_id is None and "sensor_id" in self.model_fields_set:
            _dict['sensor_id'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SpatialFilterZoneSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "workflow_uuid": obj.get("workflow_uuid"),
            "workflow_name": obj.get("workflow_name"),
            "workflow_slug": obj.get("workflow_slug"),
            "node_uuid": obj.get("node_uuid"),
            "node_name": obj.get("node_name"),
            "polygon": obj.get("polygon"),
            "point_mode": obj.get("point_mode"),
            "classes": obj.get("classes"),
            "sensor_id": obj.get("sensor_id")
        })
        return _obj


