# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TrajectoryRecipeSchema(BaseModel):
    """
    Optional kinematic scaffolding applied around the raw action.  Keeping the recipe separate from ``action`` means the same target pose can be replayed with different pre-grasp offsets / lift heights without restating the pose. MVP fields mirror what the frontend ``graspToEpisode`` adapter (P3) needs; grow the schema additively as adapters land.
    """ # noqa: E501
    pre_grasp_offset_m: Optional[Union[StrictFloat, StrictInt]] = 0.1
    lift_m: Optional[Union[StrictFloat, StrictInt]] = 0.1
    approach_axis: Optional[StrictStr] = '-z'
    fps: Optional[Union[StrictFloat, StrictInt]] = None
    __properties: ClassVar[List[str]] = ["pre_grasp_offset_m", "lift_m", "approach_axis", "fps"]

    @field_validator('approach_axis')
    def approach_axis_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['+x', '-x', '+y', '-y', '+z', '-z']):
            raise ValueError("must be one of enum values ('+x', '-x', '+y', '-y', '+z', '-z')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrajectoryRecipeSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if fps (nullable) is None
        # and model_fields_set contains the field
        if self.fps is None and "fps" in self.model_fields_set:
            _dict['fps'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrajectoryRecipeSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "pre_grasp_offset_m": obj.get("pre_grasp_offset_m") if obj.get("pre_grasp_offset_m") is not None else 0.1,
            "lift_m": obj.get("lift_m") if obj.get("lift_m") is not None else 0.1,
            "approach_axis": obj.get("approach_axis") if obj.get("approach_axis") is not None else '-z',
            "fps": obj.get("fps")
        })
        return _obj


