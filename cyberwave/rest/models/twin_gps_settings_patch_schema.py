# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TwinGpsSettingsPatchSchema(BaseModel):
    """
    Whether and how a twin's live position is derived from its GNSS fixes.  LIVE MODE ONLY -- GPS is not produced by any simulation backend, so in Simulate the twin renders from its authored pose instead.  Every field is required when enabling, and none has a server-side default, because each one changes where the twin is drawn: a rover whose ``vertical_source`` silently defaulted to ``gps_altitude`` would hover or sink by the GNSS vertical error, and a ``max_horizontal_accuracy_m`` nobody chose is a threshold nobody can be held to.  ``position_source: \"static\"`` needs no other field -- it removes the whole configuration rather than storing the string, so there is one representation of one state.
    """ # noqa: E501
    position_source: StrictStr = Field(description="'gps' derives live position from GNSS fixes; 'static' clears the configuration and returns the twin to its authored pose")
    yaw_source: Optional[StrictStr] = None
    vertical_source: Optional[StrictStr] = None
    min_fix_quality: Optional[StrictStr] = None
    max_horizontal_accuracy_m: Optional[Union[Annotated[float, Field(strict=True, gt=0.0)], Annotated[int, Field(strict=True, gt=0)]]] = None
    __properties: ClassVar[List[str]] = ["position_source", "yaw_source", "vertical_source", "min_fix_quality", "max_horizontal_accuracy_m"]

    @field_validator('position_source')
    def position_source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['gps', 'static']):
            raise ValueError("must be one of enum values ('gps', 'static')")
        return value

    @field_validator('yaw_source')
    def yaw_source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['compass_heading', 'none']):
            raise ValueError("must be one of enum values ('compass_heading', 'none')")
        return value

    @field_validator('vertical_source')
    def vertical_source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['gps_altitude', 'anchor_plane']):
            raise ValueError("must be one of enum values ('gps_altitude', 'anchor_plane')")
        return value

    @field_validator('min_fix_quality')
    def min_fix_quality_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['2d', '3d', 'dgps', 'rtk_float', 'rtk_fixed']):
            raise ValueError("must be one of enum values ('2d', '3d', 'dgps', 'rtk_float', 'rtk_fixed')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TwinGpsSettingsPatchSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if yaw_source (nullable) is None
        # and model_fields_set contains the field
        if self.yaw_source is None and "yaw_source" in self.model_fields_set:
            _dict['yaw_source'] = None

        # set to None if vertical_source (nullable) is None
        # and model_fields_set contains the field
        if self.vertical_source is None and "vertical_source" in self.model_fields_set:
            _dict['vertical_source'] = None

        # set to None if min_fix_quality (nullable) is None
        # and model_fields_set contains the field
        if self.min_fix_quality is None and "min_fix_quality" in self.model_fields_set:
            _dict['min_fix_quality'] = None

        # set to None if max_horizontal_accuracy_m (nullable) is None
        # and model_fields_set contains the field
        if self.max_horizontal_accuracy_m is None and "max_horizontal_accuracy_m" in self.model_fields_set:
            _dict['max_horizontal_accuracy_m'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TwinGpsSettingsPatchSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "position_source": obj.get("position_source"),
            "yaw_source": obj.get("yaw_source"),
            "vertical_source": obj.get("vertical_source"),
            "min_fix_quality": obj.get("min_fix_quality"),
            "max_horizontal_accuracy_m": obj.get("max_horizontal_accuracy_m")
        })
        return _obj


