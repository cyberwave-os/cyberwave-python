# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DatasetImportInitiatedSchema(BaseModel):
    """
    Returned with HTTP 200 when a zip import is initialized (signed URL).
    """ # noqa: E501
    status: Optional[StrictStr] = 'awaiting_upload'
    dataset_uuid: StrictStr
    upload_url: StrictStr
    upload_path: StrictStr
    expires_at: StrictStr
    poll_url: StrictStr
    message: Optional[StrictStr] = 'Upload the zip to the signed URL, then call POST /datasets/import/complete.'
    __properties: ClassVar[List[str]] = ["status", "dataset_uuid", "upload_url", "upload_path", "expires_at", "poll_url", "message"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['awaiting_upload']):
            raise ValueError("must be one of enum values ('awaiting_upload')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DatasetImportInitiatedSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DatasetImportInitiatedSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status") if obj.get("status") is not None else 'awaiting_upload',
            "dataset_uuid": obj.get("dataset_uuid"),
            "upload_url": obj.get("upload_url"),
            "upload_path": obj.get("upload_path"),
            "expires_at": obj.get("expires_at"),
            "poll_url": obj.get("poll_url"),
            "message": obj.get("message") if obj.get("message") is not None else 'Upload the zip to the signed URL, then call POST /datasets/import/complete.'
        })
        return _obj


