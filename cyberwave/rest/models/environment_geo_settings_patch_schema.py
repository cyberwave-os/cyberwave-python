# coding: utf-8

"""
    Cyberwave API

    Cyberwave platform REST API. Use 'Bearer YOUR_TOKEN' for authentication.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class EnvironmentGeoSettingsPatchSchema(BaseModel):
    """
    Georeference for an environment: where on Earth its origin sits.  Deliberately typed and bounded rather than a free-form dict like ``EnvironmentNavigationSettingsPatchSchema`` above. That schema's blob is fine for a transform written by a mapping bridge; it is not fine for the field that decides where on Earth a site is, and mixing the two under one endpoint would let a nav-bridge PATCH carry a geo key by accident.  Every field is required -- there is no partial merge. Updating ``latitude`` while leaving a stale ``longitude`` behind would be worse than having no anchor at all. Note that ``latitude: 0.0, longitude: 0.0`` is accepted as the real place it is; Pydantic distinguishes absent from zero, so absent stays an error rather than becoming the Gulf of Guinea.  ``heading_deg`` is the true-north bearing, in degrees clockwise, of the environment's +Y axis. See ``src/lib/geo_transform.py`` for the full frame convention.
    """ # noqa: E501
    latitude: Union[Annotated[float, Field(le=90.0, strict=True, ge=-90.0)], Annotated[int, Field(le=90, strict=True, ge=-90)]] = Field(description="WGS-84 latitude of the environment origin")
    longitude: Union[Annotated[float, Field(le=180.0, strict=True, ge=-180.0)], Annotated[int, Field(le=180, strict=True, ge=-180)]] = Field(description="WGS-84 longitude of the environment origin")
    altitude: Union[StrictFloat, StrictInt] = Field(description="Altitude in metres, in altitude_datum, at env z == 0")
    altitude_datum: StrictStr = Field(description="Datum the receiver reports altitude in. Required: geoid undulation is tens of metres, so silently mixing MSL and HAE is a 30 m error.")
    heading_deg: Union[Annotated[float, Field(le=360.0, strict=True, ge=-360.0)], Annotated[int, Field(le=360, strict=True, ge=-360)]] = Field(description="True-north bearing of the environment +Y axis, degrees clockwise")
    __properties: ClassVar[List[str]] = ["latitude", "longitude", "altitude", "altitude_datum", "heading_deg"]

    @field_validator('altitude_datum')
    def altitude_datum_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['msl', 'ellipsoid']):
            raise ValueError("must be one of enum values ('msl', 'ellipsoid')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EnvironmentGeoSettingsPatchSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EnvironmentGeoSettingsPatchSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "latitude": obj.get("latitude"),
            "longitude": obj.get("longitude"),
            "altitude": obj.get("altitude"),
            "altitude_datum": obj.get("altitude_datum"),
            "heading_deg": obj.get("heading_deg")
        })
        return _obj


